<!-- TODO: Review -->

# 親に依存するコンポーネントを作らない

## 概要

- 親が特定の実装をしていることを前提としたコンポーネントを作らない。
    - 例えば、親が `<ul>`（`List.Root`）であることを期待して、`<li>`（`List.Item`）だけのコンポーネントを作るべきではない。

## 👎 不適切な例

```tsx
// List.Root にラップされる前提になっており、
// このコンポーネントは親側の実装を知っている必要がある
export const ListItem: FC<{ label: string }> = ({ label }) => {
  return (
    <List.Item>{label}</List.Item>
  )
}
```

## 👍 適切な例

```tsx
// このコンポーネントは親が何であれこれ単体で機能でき、余計な依存がない
export const List: FC<{ items: string[] }> = ({ items }) => {
  return (
    <List.Root>
      {items.map((item, index) => (
        <List.Item key={index}>{item}</List.Item>
      ))}
    </List.Root>
  )
}
```

## なぜこのルールが必要か

前者の例のようなコンポーネントを作った場合、親側からすると `ListItem` の実装を知っていて適切なコンポーネントでラップしなければいけなくなり、`ListItem` からすると、自身を呼ぶ親コンポーネントは `List.Root` を使っている前提で実装していることになる。
いずれにせよこれは親と子に余計な依存が生じている実装になっている。子は自分の構造を自分で完結させるべきである。

→ なぜ：[最小依存性：親に依存しないコンポーネント](../../../Principles/Engineering/01_最小依存性.md)

## 例外

特になし
